<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature android:name="android.hardware.camera.any" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".cart_adapter"></activity>
        <activity android:name=".ConfirmationActivity"/>
        <activity android:name=".CartActivity" />
        <activity android:name=".show_menu" />
        <activity android:name=".customer_side_res" />
        <activity android:name=".add_menu_res" />
        <activity android:name=".NinisKitchen" />
        <activity android:name=".Chocolateroom" />
        <activity android:name=".Hardrockcafe" />
        <activity android:name=".EditorsPicksActivity" />
        <activity android:name=".MyRestaurant" />
        <activity android:name=".forgotpassword" />
        <activity android:name=".AddBusinessActivity" />
        <activity android:name=".BusinessOwnerActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".loginActivity"
            android:noHistory="true" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SearchActivity">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name=".NavigationActivity"
            android:label="@string/title_activity_navigation"
            android:theme="@style/AppTheme" />
        <activity android:name=".ListDetailsActivity" />
        <activity android:name=".GridviewActivity" />
        <activity android:name=".FoodtrucksActivity" />
        <activity android:name=".CafesActivity" />
        <activity android:name=".BuffetsActivity" />
        <activity android:name=".LatenightActivity" />
        <activity android:name=".StreetfoodActivity" />
        <activity android:name=".TiffinActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
    </application>

</manifest>